{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pytest with coverage",
      "type": "shell",
      "command": "python3 -m pytest --rootdir=. --cov=. --cov-report=xml .",
      "presentation": {
        "reveal": "never"
      }
    }
  ]
}
